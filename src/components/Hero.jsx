import React from "react";

import HeroText from "./Herotext";

import "../index.css";

const Hero = () => {
  return (
    <main className="hero">
      <HeroText />
    </main>
  );
};

export default Hero;
